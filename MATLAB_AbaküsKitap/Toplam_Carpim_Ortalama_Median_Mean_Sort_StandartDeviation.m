X=[1,2,3];
sumX=sum(X)             % toplamları
prodX=prod(X)           % çarpımları
cumsumX=cumsum(X)       % sırasıyla toplamları
cumprodX=cumprod(X)     % sırasıyla çarpımları
meanX=mean(X)           % ortalaması
medianX=median(X)       % ortanca değeri
sortX=sort(X)           % sıralanışı
stdX=std(X)             % standart sapması

B=[1,2,3;4,5,6;7,8,9];
sumB=sum(B)
prodB=prod(B)
cumsumB=cumsum(B)
cumprodB=cumprod(B)
meanB=mean(B)
medianB=median(B)
sortB=sort(B)
stdB=std(B)

N=20;
n=1:20;
toplam_n=sum(n)
carpim_n=prod(n)

% rand
% rand(n)
% rand(r,c)
% 
% randn
% randn(n)
% randn(r,c)
